! (import! &self motto)
; ! ((anthropic-agent) (user "What is a black hole?"))
! (llm (user "What is a black hole?"))

! (bind! random (py-atom random.randint))
! (random 2 98)
! (bind! input (py-atom input))
(= (in)
    (let $i (input) 
    (llm (user $i))
)
)

(= (converse $truth)
    (if $truth
        (let $input (input)
            (if (== $input "q")
                (converse False)
                (converse True)))
        "Exit")
)
! (converse True)
; ! (input)
; !((chat-gpt-agent) (user "Hello"))
; !((open-router-agent) (user "Who was the 22nd President of France?"))
